`import History from '../../../utils/history'`
`import { module, test } from 'qunit'`

module 'Unit | Utility | history'

# Replace this with your real tests.
test 'it works', (assert) ->
  assert.ok History?.truckDock
  assert.ok History?.truckScale
